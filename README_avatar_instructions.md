This file documents the avatar change.

- Image file used: `My photo.jpeg` (kept original filename)
- HTML updated: replaced text avatar with an `<img>` inside `.avatar` container.
- CSS updated: added `.avatar-img` rules for circular crop and `object-fit: cover`.

If you'd like the filename changed to `assets/avatar.jpg`, I can rename/move the file and update references.